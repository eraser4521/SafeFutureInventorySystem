@model SafeFutureInventorySystem.Models.InventoryItem
@{
    ViewData["Title"] = "Item QR Code";
}

<div class="text-center">
    <h2>QR Code for @Model.Name</h2>
    <p>Quantity: @Model.Quantity</p>
    <img src="@Url.Action("QrCodeImage", "Inventory", new { id = Model.Id })" alt="QR Code for @Model.Name" />
    <div class="mt-2">
        <a class="btn btn-outline-secondary" href="@Url.Action("QrCodeImage", "Inventory", new { id = Model.Id })">Download PNG</a>
    </div>
</div>