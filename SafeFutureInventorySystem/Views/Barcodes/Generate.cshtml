@model SafeFutureInventorySystem.Models.InventoryItem
@{
    ViewData["Title"] = "Generate Barcode";
}

<div class="text-center">
    <h2>Generate Barcode for @Model.Name</h2>
    <p>Quantity: @Model.Quantity</p>
    <img src="@Url.Action("ImageForItem","Barcodes", new { inventoryItemId = Model.Id })" alt="Barcode for @Model.Name" />

    <form asp-action="Create" method="post" class="mt-3">
        <input type="hidden" name="inventoryItemId" value="@Model.Id" />
        <button class="btn btn-primary" type="submit">Save Barcode</button>
        <a class="btn btn-secondary" href="@Url.Action("Index","Barcodes")">Cancel</a>
    </form>
</div>
